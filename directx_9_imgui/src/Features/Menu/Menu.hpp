#pragma once
namespace g_nMenu
{
	inline int tab = 0;
	//void LegitBot(); // What's the point of creating it in an outdated game?
	void Ragebot();
	void Visuals();
	void Misc();
	void Config();

	void Render(bool* p_open);
}